/* For some reason, the `swagger-ui` module declares a dependency on some
 * unknown `b` module. Even though the dependency is declared, the resolved
 * module is not passed to the factory, so it's actually unused. Odd, right?
 *
 * This module is an empty placeholder to work-around this issue.
 */
