machine:
  node:
    version: 6.1.0
  services:
    - docker

test:
  pre:
    - mkdir -p $CIRCLE_TEST_REPORTS/jest
  override:
    - npm test -- --max-parallel 1:
        environment:
          JEST_JUNIT_OUTPUT: $CIRCLE_TEST_REPORTS/jest/junit.xml
    - npm run e2e:headless
